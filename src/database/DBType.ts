export interface DataType {
  index: number;
  date: string;
  cost: boolean;
  category: string;
  amount: number;
  memo: string;
}

export interface UserType {
  id: string;
  pw: string;
}
